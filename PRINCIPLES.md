# 开发核心理念 (Core Philosophy)

> 本文档是本项目(DeepSeek Harness 桌面版 + QQ 桥)的**开发总纲**。
> 任何功能设计、代码评审、架构决策,都以此为准绳。
> 与 `mist.md`(规则)互补:理念回答"为什么这样做",规则回答"必须怎么做"。

---

## 1. 核心原则:桌面壳是主人,网页是内容

**DSH 的 Web UI 只是"一套皮肤 + 内容源",不是桌面端的主体。** 桌面端的价值在于:

- 掌控布局(顶部栏、侧栏、窗口行为)
- 调整美术资源(皮肤系统)
- 添加 Web 端没有的功能(多页面、NapCat 管理、QQ 桥)
- 更符合原生桌面软件的体验

**推论(反面教材)**:在 Web 页面 DOM 里注入 div 去"挤"布局 = 屎山。
- Web 是 React 异步渲染、hash 类名、版本漂移——注入 DOM 脆弱且每次升级都可能碎
- 正确做法:**布局由桌面壳掌控,网页作为内容嵌入**(当前 iframe 架构)
- 网页(DSH)是内容源,壳是宿主——壳决定怎么展示,不迁就网页内部结构

## 2. 🔴 原生优先:壳自己的 UI 用 Electron 原生能力,不套 web 页面

**这是最高优先级原则。壳自身的功能(设置、皮肤定制、对话框、菜单),必须优先用 Electron 原生能力实现,而不是再起一个 web HTML 窗口。**

**为什么(血泪教训)**:
- 皮肤定制窗口(web HTML)反复出问题:取消卡死、CSP 拦截、sandbox 冲突、窗口生命周期与主窗口互扰
- 根因:**
一个 Electron 应用里堆叠多个 web 页面窗口(CSP/sandbox/iframe/preload 层层叠加),版本兼容性、事件竞态、渲染线程问题层出不穷
**
- 用户原话:"你是不是又在用 web 端没做深入本地化"

**正确做法**:
- **原生菜单**做功能入口(皮肤切换、导入、主色、视图切换)
- **原生对话框**(dialog.showOpenDialog 等)做文件选择、提示
- 壳 UI 只有**一个主窗口**(shell.html),承载顶栏 + 内容(iframe/启动页)
- **不为壳自己的小功能开独立 web 窗口**——除非确有必要且证明 web 方案更优

**自由度**:用户要的"高度定制化"是**用原生能力给用户自由度**(导入自己的图、选自己的色、换自己的布局),不是"用 web 页面堆功能"。

## 3. 用户导向:桌面端存在的意义

用户原话:"我要是啥都按照 web 端照搬我为啥不直接用 web 端?"
- 桌面端的每一项功能,都要回答"为什么必须在桌面端做"
- 做不到这一点的功能,不做
- 每次改动前问:"这能让桌面端比 Web 端好在哪里?"

## 4. 精简至上(与 mist.md 规则 5 一致)

- 删除 > 修改 > 添加
- 不堆叠冗余机制、不过度设计
- 反面案例:退出讨论模式本应"停止注入提示词",却新增"恢复正常"提示词 → 被纠正
- **同样适用于架构**:能用原生菜单解决的,不建 web 窗口;能一个窗口解决的,不开多个

## 5. 稳定性优先:不猜,要探明

- 涉及运行时结构(Web DOM、第三方 UI)时,**用探针实测**再动手,不靠猜
- 但探针结果要**自动存取复用**,不让用户反复手动跑
- 更优:架构上**不依赖第三方结构**(iframe 方案就不需要探 DSH 内部 DOM)

## 6. 配置驱动,热生效

- 人设、能量、群配置等全部配置化(补丁 YAML),改配置 HMR 即时生效
- 代码逻辑改动(.mjs)需重启,重启方式要可靠(当前由用户手动,后续优化)

## 7. 并发正确性:串行优先

- 消息处理、状态变更涉及竞态时,**per-群串行队列**根治
- 反面案例:消息并发导致冷却失效 + 图片误删(已修,见 hist.md 记录 101-103)

## 8. 检查即契约(与 mist.md 规则 6 一致)

- 代码优化提交前**三遍检查**:语法+模块 / 功能冒烟 / 生命周期
- DSH 启动相关(apply 入口、依赖顺序、disposer)重点检查
- 必须基于**要提交的那份代码**验证,不测旧版本(假阳性教训)

## 9. 版本即产物指纹(与 mist.md 规则 7 一致)

- 每次更新迭代版本号,产物必须与源码一致(md5 验证)
- 避免"改了代码但打包的是旧产物"导致验证错版本

## 10. 长期记忆

- 重大决策、教训、根因分析 → 写入 `HANDOFF.md` / `hist.md` / `mist.md`
- 让任何会话/协作者都能无缝接管

---

*维护:本文档随项目演进更新。改动时同步检查上述理念是否仍适用。*
